# --- API + agents ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0
langgraph>=0.2.0
langchain-core>=0.3.0
ddgs>=6.0            # keyless DuckDuckGo search for the Researcher agent

# --- real analysis engine (Phase 4) ---
pandas>=2.0.0
openpyxl>=3.1          # Excel (.xlsx/.xls) upload
pyarrow>=15.0          # Parquet upload
numpy<2            # keeps SHAP + compiled extensions consistent
scikit-learn>=1.3.0
flaml[automl]>=2.1.0
shap<0.50          # 0.46.x works with numpy<2
statsmodels>=0.14  # time-series forecasting (Holt-Winters / ETS)
lifelines>=0.27    # survival analysis (Kaplan-Meier / Cox PH)

# --- sandbox (Phase 3) + RAG (Phase 5) ---
RestrictedPython>=7.0           # zero-dependency in-process executor (fallback)
e2b-code-interpreter>=1.0.0     # optional hardened microVM executor (used when E2B_API_KEY is set)
chromadb>=0.5.0
opencv-python-headless<4.10  # SHAP imports cv2; headless build for servers
